const form=document.querySelector('#ask-form');const result=document.querySelector('#result');
document.querySelector('#as-of').value=new Date().toISOString().slice(0,10);
form.addEventListener('submit',async(event)=>{event.preventDefault();result.hidden=false;document.querySelector('#status').textContent='Retrieving authorised policy evidence…';document.querySelector('#answer').textContent='';document.querySelector('#citations').replaceChildren();
try{const response=await fetch('/api/ask',{method:'POST',headers:{'content-type':'application/json','X-Demo-User-ID':document.querySelector('#user-id').value,'X-Demo-User-Groups':document.querySelector('#groups').value},body:JSON.stringify({question:document.querySelector('#question').value,as_of:document.querySelector('#as-of').value,top_k:5})});const data=await response.json();if(!response.ok)throw new Error(data.detail||'Request failed');document.querySelector('#status').textContent=`Backend: ${data.backend} · Correlation: ${data.correlation_id}`;document.querySelector('#answer').textContent=data.answer;for(const citation of data.citations){const li=document.createElement('li');li.textContent=`${citation.document_title} v${citation.version}, section ${citation.section_number} (${citation.chunk_id})`;document.querySelector('#citations').append(li)}}catch(error){document.querySelector('#status').textContent='Request failed';document.querySelector('#answer').textContent=error.message}});
